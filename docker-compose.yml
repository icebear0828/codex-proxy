services:
  codex-proxy:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    restart: unless-stopped
    environment:
      - NODE_ENV=production

# Grok Claude profile (port 8088)

See `local.yaml.example` and `api-keys.json.example`. Full WSL/systemd steps are in
`scripts/install_grok_claude_systemd.sh` header comments and codex-lb-go
`scripts/ensure_grok_stack_wsl_boot.sh`.